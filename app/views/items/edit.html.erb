<h1>商品編集</h1>


<%= form_for(@item) do |f| %>
	<p>商品画像</p>
	<%= f.attachment_field :image %>
	<p>商品名</p>
	<%= f.text_field :name %>
	<p>商品名</p>
	<%= f.text_area :caption %>
	<%= f.submit "Update" %>
<% end %>